In this example, we want to push wlogout into git. Camni lah

mv ~/.config/wlogout ~/dotfiles/.config/
ln -s ~/dotfiles/.config/wlogout ~/.config/wlogout
cd ~/dotfiles
git add .config/wlogout
git commit -m "Add wlogout config"
git push