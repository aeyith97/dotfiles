In this example, we want to push aeyith into git. Camni lah

mv ~/.config/aeyith ~/dotfiles/.config/
ln -s ~/dotfiles/.config/aeyith ~/.config/aeyith
cd ~/dotfiles
git add .config/aeyith
git commit -m "Add aeyith config"
git push